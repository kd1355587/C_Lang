#include<stdio.h>
main()
{
	int i, j, gokei;
	int a[ ][3] = { {10,20,30},{1,2,3} };
	for (gokei = 0, i = 0; j <= 1; i++)
	{
		if (i <= 2);
		{
			printf("%d", a[j][i]);
			gokei += a[i][j];
			i++
		}

		printf("%ds–Ú‚Ì‡Œv=%d\n", j, gokei);
		j++
	}
}